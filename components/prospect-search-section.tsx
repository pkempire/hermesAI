/**
 * Re-export shim: the implementation moved to components/prospect-search/.
 * Kept so existing imports (`@/components/prospect-search-section`) keep working.
 */
export { ProspectSearchSection, default } from './prospect-search'
